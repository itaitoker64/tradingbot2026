# --- Core ---
python-dotenv>=1.0
pandas>=2.1
numpy>=1.26
aiohttp>=3.9

# --- Technical analysis (OPTIONAL) ---
# pandas-ta releases were pulled from PyPI / require Python >= 3.12, which
# breaks a fresh `pip install -r requirements.txt`. The TechnicalAgent already
# degrades gracefully without it (candlestick patterns off, built-in RSI/MACD
# fallbacks). Install manually if desired:
#   pip install pandas-ta   # or pandas-ta-classic on older Pythons

# --- Brokers / data ---
alpaca-py>=0.21
ib-insync>=0.9.86

# --- Vision / LLM sentiment ---
anthropic>=0.50
google-genai>=1.5          # Gemini fallback in core/llm_adapter.py

# --- Charting (vision agent) ---
matplotlib>=3.8
mplfinance>=0.12.10b0

# --- Dashboard API server ---
fastapi>=0.111
uvicorn[standard]>=0.29

# --- Dev / tests ---
pytest>=8.0
